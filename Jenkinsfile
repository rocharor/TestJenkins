pipeline {
    agent any
    stages {
        stage('---clean---') {
            steps {
                sh "ls -la"
            }
        }
        stage('--test--') {
            steps {
                sh "ls -la"
            }
        }
        stage('--package--') {
            steps {
                sh "ls -la"
            }
        }
    }
}
